{
  "name": "json-comparator",
  "version": "0.1.0",
  "private": true,
  "description": "Zero-dependency JSON comparator. This package.json provides the dev toolchain (tests + lint) only; the web app stays a single HTML file openable via file:// with no build step.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@eslint/js": "^9.13.0",
    "eslint": "^9.13.0",
    "globals": "^15.11.0",
    "vitest": "^2.1.3"
  }
}
